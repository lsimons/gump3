A MySQL database plays a rather vital part in Gump3. It is used by pygump as the main metadata
store during runs, as the means of communication between pygump and dynagump, and as the main
storage for dynagump as well.

